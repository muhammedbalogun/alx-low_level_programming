#include <stdio.h>

/**
 * main - program that prints all arguments it receives
 * @argc: arguments supply
 * @argv: pointer to the arguments.
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
		printf("%s\n", argv[c]);
	return (0);
}
